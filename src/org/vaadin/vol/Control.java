package org.vaadin.vol;

public enum Control {
    ArgParser, Navigation, LayerSwitcher, ScaleLine, PanZoomBar, 
    PanZoom, NavToolbar, OverviewMap, MousePosition, ZoomToMaxExtent,
    /**
     * Supported from OpenLayers 2.11.
     */
    TouchNavigation, Attribution, ZoomPanel, DrawFeature, TransformFeature
}
